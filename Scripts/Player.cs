using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : MonoBehaviour
{    
    public string unitName;
    public int score;
    public int level;
        
    //player power up - for the sequel of the game
    public bool physicPowerWallCollider10;
    public bool enemyFrictionDouble;
    public bool doubleForce;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
